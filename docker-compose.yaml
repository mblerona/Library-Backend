services:
  db:
    image: postgres:17.4
    restart: always
    environment:
      POSTGRES_USER: emtlab
      POSTGRES_PASSWORD: emtlab
      POSTGRES_DB: emtlab
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5434:5432"
volumes:
  pgdata:
